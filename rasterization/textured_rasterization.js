// Textured Rasterization
// nectarboy | 2021
// * Rasterization of textured triangles using vertices and UV maps.

// --- Rasterization
// @param 'scene' is a Scene object.
// @param 'verts' is an Array full of Objects containing 'x' and 'y' coordinates in Integers.
// @param 'uv' is uhh umm ...................
// @param 'data' is an ImageData object representing the texture.
function rasterizeSolid(scene, verts, uv, data) {
    rasterize(scene, verts,
        function(x, y) {
            
        }
    );
}